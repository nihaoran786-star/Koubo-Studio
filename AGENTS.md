# 口播智能体项目规则

## 回复与工作方式

- 总是用中文回复。
- 开工前先读 `docs/CONTEXT.md`、相关代码、配置和测试，以代码事实校正文档假设。
- 不做无关重构，不回滚用户或其他代理已有改动。
- 每轮改动都要说明目标、模块边界、涉及文件和验证结果。
- 优先保证真实链路可运行、可恢复、可验证，再增加功能。

## 产品定位

这是一个 local-first 的 Windows 数字人口播制作应用，主流程固定为五个阶段：

```text
AI 对话生成文案
→ 选择音色或克隆声音
→ 选择形象或上传形象素材并生成数字人视频
→ 本地简单剪辑或由 AI Agent 生成精剪方案
→ 打开本机可见浏览器辅助发布
```

主 App 保持轻量。模型、GPU 服务、媒体处理器和浏览器自动化都是可替换 runtime，不得成为基础 UI 的强耦合实现。

## 当前主线

按以下顺序推进，不横向扩张：

1. 删除旧外部发布系统的代码、脚本、配置、测试和验收残留，不保留兼容层。
2. 收敛项目状态与 workspace，以单一创作项目串联文案、声音、数字人、剪辑和发布准备。
3. 声音和形象只展示真实可用素材；支持本地导入、复用和删除。
4. 做实基于 ffmpeg 的本地字幕、裁切、音量、背景音乐、片头片尾、封面与导出。
5. AI Agent 只生成受控剪辑计划，由同一个本地剪辑模块执行，不直接拼接任意系统命令。
6. 优化五步创作 UI、启动性能、运行时反馈与失败恢复。
7. 最后实现抖音和小红书的可见浏览器发布自动化。

旧外部发布系统已经退出产品方向：相关实现是明确删除项，不再修复、迁移或继续验收。

## 模块和依赖边界

推荐依赖方向：

```text
UI
→ hook / client
→ API route
→ module service
→ adapter
→ local or remote runtime
```

模块职责：

- 文案模块：AI 对话、文案版本、确认状态。
- 声音模块：音色素材、参考音频、IndexTTS2 任务与音频结果。
- 数字人模块：形象素材、Duix/HeyGem 任务与视频结果。
- 剪辑模块：受控剪辑计划、ffmpeg 执行、字幕和最终成片。
- 发布模块：生成发布文案和素材包，驱动本机可见浏览器。
- workspace 模块：项目目录、素材、产物、状态恢复和路径安全。
- Tauri 壳：桌面窗口、本地后端生命周期和系统能力入口。

必须遵守：

- UI 只渲染明确 state 并发送用户意图，不访问 SDK、文件系统、进程命令、浏览器 cookie 或平台密码。
- API route 只做请求校验和响应转换，不堆业务判断。
- adapter 隔离 IndexTTS2、Duix/HeyGem、ffmpeg、浏览器等外部变化。
- 错误使用稳定的 `status/source/error.code/error.message`，不依赖字符串猜测。
- 所有本地路径必须限制在当前 workspace 或明确选择的素材库根目录内。

## 创作项目状态

页面不得手工推断底层产物来源。每个阶段至少明确表达：

```text
idle | needs_input | queued | running | ready | failed
```

创作项目是唯一业务真相，包含五阶段状态和当前有效产物引用。不要把 `[]`、空字符串或按钮禁用当作业务状态，也不要让浏览器存储形成第二套项目真相。

## Runtime 分层

- 基础层：Next.js/React UI、Tauri、本地后端、AI Provider、workspace。
- 生成层：IndexTTS2、Duix/HeyGem，可在本机或远程机器运行。
- 剪辑层：本机 ffmpeg/ffprobe 和受控剪辑执行器。
- 发布层：本机可见浏览器与平台 adapter。

设置页只负责检测、配置、启动或连接 runtime，并渲染 `profile/status/source/error`。重模型和 Docker 不是基础安装的硬前置。

## 浏览器发布规则

- 首批平台只做抖音和小红书。
- 使用本机可见浏览器，允许用户随时接管。
- 用户监督登录、扫码、验证码、风控和账号选择；应用不得保存平台密码。
- 自动化可以打开发布页、选择视频、填写标题、正文和标签。
- 最终提交发布必须停下来由用户监督和确认；未经用户明确授权不得自动点击最终发布按钮。
- 平台 DOM 变化必须收敛在各平台 adapter，不得散落到页面组件。

## 开工前 Architecture Gate

小任务至少说明目标、允许改动范围和验证方式。涉及跨模块、状态模型、API、文件系统、外部 runtime 或浏览器自动化时，开工前完整回答：

```text
1. 本轮目标是什么？
2. 改动属于哪个模块？
3. UI / route / service / adapter / external runtime 的边界是什么？
4. 哪些文件允许修改？
5. 哪些文件禁止加入业务判断？
6. 状态和数据流是什么？
7. 需要新增或更新哪些测试？
8. 若必须跨越计划外边界，先停止并说明原因。
```

## 完成前 Coupling Review Gate

结束前检查：

```text
1. 页面、入口和大组件是否只组合与渲染？
2. 状态模型是否明确且只有一个事实源？
3. UI 是否避免推断底层来源？
4. route / service / adapter 是否返回稳定状态和错误？
5. 外部变化是否被 adapter 隔离？
6. 是否混入无关改动？
7. diff 是否可作为一个小 PR 理解？
8. 测试是否覆盖模块接口和关键失败路径？
```

## 测试和验证

- 每轮运行覆盖风险的最小验证；能跑自动测试就运行。
- 前端改动至少执行类型检查，并对关键页面做浏览器验证。
- service/adapter 改动优先覆盖模块接口、路径安全、超时、取消和错误归类。
- 媒体产物必须验证存在、非空、可被 ffprobe 读取，并与当前项目阶段关联。
- 删除旧能力后至少执行引用扫描、`pnpm typecheck`、相关测试和 `pnpm build`。
- 浏览器发布测试默认停在最终提交前，真实发布只能在用户监督下执行。

## 项目标记

- 标记：`MAIN-KOUBO-AGENT`
- 路径：`D:\codex\口播智能体`
- 类型：Next.js / React / TypeScript / Tauri 2 Windows 桌面应用。
- Node 要求：`>= 22.19.0`。
- 常用验证：`pnpm typecheck`、`pnpm test`、`pnpm build`、`pnpm desktop:build`。

